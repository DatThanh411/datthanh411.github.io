  // Bài tập function buổi 2
            // Bài 1
            function theKi(x) {
                return Math.ceil(x / 100);
            }
        
        // Bài 2
            function catChuoi(x) {
                return x.slice(0,10) + "...";
            }
        
        // Bài 3 
            function vietHoa(x) {
                return x[0].toUpperCase() + x.slice(1).toLowerCase();
            }
        
        // Bai 4 
            function catString(x) {
                return x.split(" ").join("-").toLowerCase();
            }